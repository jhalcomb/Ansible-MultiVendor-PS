{# CISCO NXOS HAS 2 SPACE OF INDENTATION #}
{% for name, vid in vlans.items() %}
vlan {{ vid }}
  name {{ name | upper }}
interface Vlan{{ vid }}
  description {{ name | upper }} VLAN
  ip address 10.0.{{ vid }}.{{ inventory_hostname[1:] }}/24
{% endfor %}
